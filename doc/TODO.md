- 여자 캐릭터를 새로 찾는다 (더 예쁜 캐릭으로)
- 남녀 애니메이션을 구분한다 (walk_female, walk_male, etc)
- locomotion.glb, 등에 스킨드 메쉬를 넣어서 애니메이션을 눈으로 확인할 수 있게 한다
- 죽으면 경험치 드랍을 구현한다
- AI NPC를 구현한다
  - 애셋을 구한다
  - 세계관을 작성한다
  - LLM에 넘긴다
- 생성형 던전 시스템을 만든다
  - 모듈형 애셋을 만든다
  - 미로를 생성하는 시스템을 만든다
- map editor를 만든다
- height map -> terrain 생성 시스템을 만든다
- 간단한 집 모델을 구한다
- 실내에 들어가면 전경과 지붕을 날리는 시스템을 만든다
- 터레인에 동식물을 배치한다
- 강물을 구현한다
- 바다를 구현한다
- 배를 구현한다
